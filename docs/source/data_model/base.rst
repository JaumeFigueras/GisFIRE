Base
====

The declarative base class that every GisFIRE ORM model inherits from. See
:doc:`../data_model` for how it fits into the data model.

.. autoclass:: src.data_model.Base
   :show-inheritance:
